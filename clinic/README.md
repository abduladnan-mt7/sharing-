# Pearl Dental Clinic Website

This contains everything you need to run the website locally.

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Set your `API_KEY` in [.env](.env) (refer to `.env.example`)
3. Run the app:
   `npm run dev`
