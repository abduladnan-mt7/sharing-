export interface NavItem {
  label: string;
  href: string;
}

export interface Benefit {
  title: string;
  description: string;
}

export interface Ingredient {
  name: string;
  formula?: string;
  description: string;
}